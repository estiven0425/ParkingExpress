<UserControl d:DesignHeight="620"
             d:DesignWidth="540"
             mc:Ignorable="d"
             x:Class="ParkingExpress.Controls.Dashboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ListView Grid.Column="0"
                  Grid.Row="0"
                  Grid.RowSpan="2"
                  ItemsSource="{Binding Recibos}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.DetalleReciboCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                            CommandParameter="{Binding}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Ingreso}"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Salida}"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Area.Nombre}"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Vehiculo.Placa}"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Estado}"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
